#!/usr/bin/make -f

%:

	dh ${@}

override_dh_auto_install:

	$(MAKE) DESTDIR=$(CURDIR)/debian/gnome-themes-gnamon install

override_dh_gconf:

	dh_gconf --priority=20

override_dh_installgsettings:

        dh_installgsettings --priority=20
